package com.example.mathbomb;
import java.util.Date;

import android.content.Context;
import android.util.Log;

public class Easy_SaveScore {
	private Context mAppContext;

	public Easy_SaveScore(Context appContext) {
		this.mAppContext = appContext;
	}
	
	public void saveScore(int score, Date date) throws Exception {
		Easy_Record mRecord = new Easy_Record();
		
		mRecord.setScore(Integer.toString(score));
		mRecord.setDate(date);
		SingleRecordEasy.get(mAppContext).addDetails(mRecord);
		SingleRecordEasy.get(mAppContext).saveDetails();
		Log.i("SaveScore",score+"");	
		Log.i("SaveScore",date+"");
	}
	
	
}
